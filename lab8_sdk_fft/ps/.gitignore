# files/folders generated by launching sdk
.Xil
*.log
.metadata
.sdk
RemoteSystemsTempFiles
webtalk

# keep only system.hdf and .project in hw projects
*_hw_platform_*/*
!*_hw_platform_*/.project
!*_hw_platform_*/*.hdf

# keep only .mss, .project, .cproject, .sdkproject, and Makefile in bsp projects
*_bsp/*
!*_bsp/*.mss
!*_bsp/Makefile
!*_bsp/.project
!*_bsp/.cproject
!*_bsp/.sdkproject

# keep only src, .project, .cproject in app projects
Debug
/*/src/README.txt
/*/src/Xilinx.spec